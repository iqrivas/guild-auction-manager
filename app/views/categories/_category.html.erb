<div id="<%= dom_id category %>">
  <p>
    <strong>Id:</strong>
    <%= category.id %>

    <strong>Name:</strong>
    <%= category.name %> |
    <%= link_to "Edit", edit_category_path(category.id) %>
  </p>

</div>
